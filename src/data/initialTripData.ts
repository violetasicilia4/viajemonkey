import type { TripItem } from "../types/trip";

// Intentionally empty: no placeholder flights/stays/reservations are invented here.
// Populate this array with the user's real trip data (or remove seeding entirely
// once the app writes items through the UI) before calling seedInitialDataIfEmpty().
export const initialTripData: TripItem[] = [];
